package com.fitnesstracker.monolith.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration class for conditional services.
 * MongoDB configuration temporarily removed for deployment stability.
 */
@Configuration
public class ConditionalServiceConfig {
    // MongoDB configuration will be re-added when services are properly configured
}
